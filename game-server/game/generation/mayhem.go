package generation

import "bomberman-game-server/shared"

// TODO: This is supposed to create that one map where the map basically consists of only powerups in the middle
// and the with a one block wall on the edge so that u only have to destroy one wall to get to all powerups
func GenerateMayhem(width, height int) *shared.Field {
	return nil
}
